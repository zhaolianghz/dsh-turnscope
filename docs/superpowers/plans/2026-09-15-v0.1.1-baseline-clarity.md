# V0.1.1 — Baseline-Dirty Clarity

Implementation plan for `docs/superpowers/specs/2026-09-15-v0.1.1-baseline-clarity.md`.

Seven bite-sized tasks, ordered so the wire shape lands before the renderer
touches it, and the locale strings land before the renderer uses them.

## Task 1 — API contract: split the count

**File**: `src/shared/contracts/api.ts`

- Add `agentChangeCount: number` and `baselineChangeCount: number` to `TurnSummaryDto` (lines 213-216).
- Add the same two fields to `EvaluateSafetyData` (lines 320-321).
- Bump `API_VERSION` (line ~60) from `3` to `4`.

**Verify**: `pnpm typecheck` clean.

## Task 2 — Server: compute both counts

**File**: `src/host/query/service.ts`

- In `summarize(...)` (line 107-122), compute the two counts from `result.changeSet?.changes ?? []`:
  - `agentChangeCount` = rows where `kind !== 'noop' && !baseline`
  - `baselineChangeCount` = rows where `kind !== 'noop' && baseline`
  - `changeCount` = sum of the two
- In `evaluateSafety(...)` (line 187), return all three counts in the data payload.

**Verify**: `pnpm typecheck` clean.

## Task 3 — Server tests: assert both counts

**File**: `tests/host/query/service.spec.ts`

- Extend the existing `listTurns` and `evaluateSafety` tests to assert `agentChangeCount` and `baselineChangeCount` are correct.
- Add a new test case: a turn whose change set has 3 agent edits and 5 baseline-dirty paths — assert the two counts come out as 3 and 5.

**Verify**: `pnpm test tests/host/query` green.

## Task 4 — Locales: three new keys per language

**File**: `src/client/locales.ts`

- zh (`zh` object):
  - `summary.agentChanges`: `本轮改动`
  - `summary.baselineChanges`: `本轮开始时已脏`
  - `summary.baselineChangesShort`: `+{count} 已脏`
- en (`en` object):
  - `summary.agentChanges`: `Changed this turn`
  - `summary.baselineChanges`: `Already dirty at turn start`
  - `summary.baselineChangesShort`: `+{count} dirty`

**Verify**: `pnpm typecheck` clean.

## Task 5 — Renderer: split the headline

**Files**:
- `src/client/TurnscopeView.tsx`
- `src/client/TurnDetail.tsx`

- `TurnscopeView.tsx` line 150: change the `<dd>` for `summary.changes` to render `summary.agentChangeCount`. When `summary.baselineChangeCount > 0`, append a `<span className="turnscope-baseline">{t('summary.baselineChangesShort', { count: summary.baselineChangeCount })}</span>` next to the number.
- `TurnDetail.tsx` (the count summary `dl`): split the existing `变更文件` row into two:
  - `summary.agentChanges` + `summary.agentChangeCount`
  - when `summary.baselineChangeCount > 0`, render a second row for `summary.baselineChanges` + `summary.baselineChangeCount`.

**Verify**: `pnpm typecheck` clean.

## Task 6 — Client tests: chip appears only when non-zero

**File**: `tests/client/recovery-section.client.spec.tsx` or new `tests/client/turn-card.client.spec.tsx` — pick the file that already hosts card-rendering tests; otherwise create a sibling file.

- Add a test that mounts a `TurnscopeView` with a recorded turn whose `summary.agentChangeCount = 3` and `summary.baselineChangeCount = 5`. Assert the rendered DOM has the `+5 已脏` chip.
- Add a second test with `baselineChangeCount = 0`. Assert the chip is **absent**.

**Verify**: `pnpm test tests/client` green.

## Task 7 — Architecture guard + full verification

- Re-run `pnpm typecheck` and `pnpm test`. Both clean, 589 + new tests passing.
- Update the spec file's `What success looks like` section to remove the draft framing (no behaviour change; the success criteria already match what the implementation produces).
- Commit. No tag bump — V0.1.1 ships by direct commit on the existing branch, no release ceremony; the README changelog gets a single-line addition at publish time.

**Verify**: `git log --oneline -3` shows the work on top of `ae08d5c`.